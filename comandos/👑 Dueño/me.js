const Discord = require("discord.js");
module.exports = {
    name: "status",
    aliases: [],
    desc: "Sirve para ver editar el estado de un usuario xd",
    owner: true,
    run: async (client, message, args, prefix, idioma) => {
        
    }
}